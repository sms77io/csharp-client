namespace Sms77.Api.Library.Status {
    public enum StatusCode {
        DELIVERED,
        NOTDELIVERED,
        BUFFERED,
        TRANSMITTED,
        ACCEPTED,
        EXPIRED,
        REJECTED,
        FAILED,
        UNKNOWN
    }
}